apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-ingress-controller
  namespace: kaneo
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    # Allow Traefik ingress controller
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              app.kubernetes.io/name: traefik
      ports:
        - protocol: TCP
          port: 5173
        - protocol: TCP
          port: 1337
    # Allow internal communication within namespace
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kaneo
      ports:
        - protocol: TCP
          port: 5173
        - protocol: TCP
          port: 1337
        - protocol: TCP
          port: 5432
