---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kindle-sender
  namespace: media
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: "4.6.2"
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 30m
  install:
    createNamespace: false
    remediation:
      retries: 3
    timeout: 15m
  upgrade:
    remediation:
      retries: 3
    timeout: 15m
  values:
    controllers:
      kindle-sender:
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              repository: ghcr.io/blazepower/kindle-sender
              tag: v1.3.0
            ports:
              - name: metrics
                containerPort: 9090
                protocol: TCP
            env:
              - name: WATCH_PATH
                valueFrom:
                  configMapKeyRef:
                    name: kindle-sender-config
                    key: WATCH_PATH
              - name: SCAN_INTERVAL
                valueFrom:
                  configMapKeyRef:
                    name: kindle-sender-config
                    key: SCAN_INTERVAL
              - name: MAX_FILE_SIZE_MB
                valueFrom:
                  configMapKeyRef:
                    name: kindle-sender-config
                    key: MAX_FILE_SIZE_MB
              - name: FILE_EXTENSIONS
                valueFrom:
                  configMapKeyRef:
                    name: kindle-sender-config
                    key: FILE_EXTENSIONS
              - name: DATABASE_PATH
                valueFrom:
                  configMapKeyRef:
                    name: kindle-sender-config
                    key: DATABASE_PATH
              - name: MAX_BOOKS_PER_HOUR
                valueFrom:
                  configMapKeyRef:
                    name: kindle-sender-config
                    key: MAX_BOOKS_PER_HOUR
              - name: SMTP_HOST
                valueFrom:
                  secretKeyRef:
                    name: kindle-sender
                    key: SMTP_HOST
              - name: SMTP_PORT
                valueFrom:
                  secretKeyRef:
                    name: kindle-sender
                    key: SMTP_PORT
              - name: SMTP_USER
                valueFrom:
                  secretKeyRef:
                    name: kindle-sender
                    key: SMTP_USER
              - name: SMTP_PASSWORD
                valueFrom:
                  secretKeyRef:
                    name: kindle-sender
                    key: SMTP_PASSWORD
              - name: KINDLE_EMAIL
                valueFrom:
                  secretKeyRef:
                    name: kindle-sender
                    key: KINDLE_EMAIL
              - name: SENDER_EMAIL
                valueFrom:
                  secretKeyRef:
                    name: kindle-sender
                    key: SENDER_EMAIL
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              runAsNonRoot: true
              runAsUser: 1000
              runAsGroup: 1000
              capabilities:
                drop:
                  - ALL
            resources:
              requests:
                cpu: "25m"
                memory: "128Mi"
              limits:
                cpu: "500m"
                memory: "512Mi"

    persistence:
      data:
        existingClaim: kindle-sender-data
        globalMounts:
          - path: /data
      media:
        type: hostPath
        hostPath: /media/rishik/Expansion
        globalMounts:
          - path: /media
            readOnly: true

    service:
      app:
        controller: kindle-sender
        ports:
          metrics:
            port: 9090
            protocol: TCP

    serviceMonitor:
      app:
        enabled: true
        serviceName: kindle-sender-app
        endpoints:
          - port: metrics
            scheme: http
            path: /metrics
            interval: 30s

    defaultPodOptions:
      nodeSelector:
        kubernetes.io/hostname: rishik-worker1
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
