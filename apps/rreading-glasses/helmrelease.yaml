---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rreading-glasses
  namespace: media
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: "3.5.1"
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 30m
  install:
    createNamespace: false
    remediation:
      retries: 3
    timeout: 15m
  upgrade:
    remediation:
      retries: 3
    timeout: 15m
  values:
    controllers:
      rreading-glasses:
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              repository: docker.io/blampe/rreading-glasses
              tag: hardcover
            command:
              - /main
            args:
              - "serve"
              - "--verbose"
            ports:
              - name: http
                containerPort: 8788
                protocol: TCP
            env:
              - name: POSTGRES_HOST
                value: "rreading-glasses-postgresql"
              - name: POSTGRES_DATABASE
                value: "rreading_glasses"
              - name: POSTGRES_USER
                value: "rreading_user"
              - name: POSTGRES_PASSWORD
                valueFrom:
                  secretKeyRef:
                    name: rreading-glasses-credentials
                    key: POSTGRES_PASSWORD
              - name: HARDCOVER_AUTH
                valueFrom:
                  secretKeyRef:
                    name: rreading-glasses-credentials
                    key: HARDCOVER_API_KEY
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              runAsNonRoot: true
              runAsUser: 1000
              runAsGroup: 1000
              capabilities:
                drop:
                  - ALL
            resources:
              requests:
                cpu: "50m"
                memory: "128Mi"
              limits:
                cpu: "200m"
                memory: "256Mi"

    service:
      app:
        controller: rreading-glasses
        type: ClusterIP
        ports:
          http:
            port: 8080
            targetPort: 8788

    defaultPodOptions:
      nodeSelector:
        kubernetes.io/hostname: rishik-worker1
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
