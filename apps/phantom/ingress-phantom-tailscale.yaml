apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: phantom-tailscale
  namespace: siloarr
  annotations:
    tailscale.com/tags: "tag:k8s-operator"
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: whisparr
      port:
        number: 6969
  tls:
    - hosts:
        - phantom
