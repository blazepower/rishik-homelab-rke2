apiVersion: apps/v1
kind: Deployment
metadata:
  name: sure-finance-postgresql
  namespace: sure-finance
  labels:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/instance: sure-finance-postgresql
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: postgresql
      app.kubernetes.io/instance: sure-finance-postgresql
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgresql
        app.kubernetes.io/instance: sure-finance-postgresql
    spec:
      containers:
        - name: postgresql
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
              name: postgresql
              protocol: TCP
          env:
            - name: POSTGRES_USER
              value: maybe
            - name: POSTGRES_DB
              value: maybe
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: sure-credentials
                  key: postgres-password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - maybe
                - -d
                - maybe
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 6
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - maybe
                - -d
                - maybe
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 6
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: sure-finance-postgresql-data
---
apiVersion: v1
kind: Service
metadata:
  name: sure-finance-postgresql
  namespace: sure-finance
  labels:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/instance: sure-finance-postgresql
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgresql
      protocol: TCP
      name: postgresql
  selector:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/instance: sure-finance-postgresql
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sure-finance-postgresql-data
  namespace: sure-finance
  labels:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/instance: sure-finance-postgresql
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 4Gi
