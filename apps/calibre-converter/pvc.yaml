---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-converter-data
  namespace: media
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Mi
