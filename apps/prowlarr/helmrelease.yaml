---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: prowlarr
  namespace: media
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: "3.5.1"
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 30m
  install:
    createNamespace: false
    remediation:
      retries: 3
    timeout: 15m
  upgrade:
    remediation:
      retries: 3
    timeout: 15m
  values:
    controllers:
      prowlarr:
        annotations:
          reloader.stakater.com/auto: "true"

        initContainers:
          init-config:
            image:
              repository: busybox
              tag: latest
            command:
              - /bin/sh
              - -c
              - |
                echo "waiting for backup to be uploaded..."

                if [ -f "/config/config.xml" ]; then
                  echo "Config already exists, exiting."
                  exit 0
                fi

                # wait for backup to be manually copied
                while ! ls /config/*.tgz >/dev/null 2>&1; do sleep 2; done;

                BACKUP_FILE=$(ls /config/*.tgz 2>/dev/null | head -n1)
                tar -xvzf "$BACKUP_FILE" -C /config
                rm "$BACKUP_FILE"

                echo "Done."

        containers:
          app:
            image:
              repository: lscr.io/linuxserver/prowlarr
              tag: latest
            env:
              - name: PUID
                valueFrom:
                  configMapKeyRef:
                    name: prowlarr-config
                    key: PUID
              - name: PGID
                valueFrom:
                  configMapKeyRef:
                    name: prowlarr-config
                    key: PGID
              - name: TZ
                valueFrom:
                  configMapKeyRef:
                    name: prowlarr-config
                    key: TZ
            resources:
              requests:
                cpu: "100m"
                memory: "256Mi"
              limits:
                cpu: "1000m"
                memory: "1Gi"

    service:
      app:
        controller: prowlarr
        ports:
          http:
            port: 9696

    persistence:
      config:
        existingClaim: prowlarr-config
        globalMounts:
          - path: /config

    defaultPodOptions:
      nodeSelector:
        kubernetes.io/hostname: rishik-worker1
