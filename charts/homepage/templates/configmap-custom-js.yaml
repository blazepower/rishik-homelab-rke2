apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "homepage.fullname" . }}-custom-js
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "homepage.labels" . | nindent 4 }}
data:
  custom.js: |
    {{- toYaml (.Values.config.customJS | default "") | nindent 4 }}
