apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "homepage.fullname" . }}-custom-css
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "homepage.labels" . | nindent 4 }}
data:
  custom.css: |
    {{- toYaml (.Values.config.customCSS | default "") | nindent 4 }}
