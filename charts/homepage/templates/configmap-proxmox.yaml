apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "homepage.fullname" . }}-proxmox
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "homepage.labels" . | nindent 4 }}
data:
  proxmox.yaml: |
    {{- toYaml (default dict .Values.config.proxmox) | nindent 4 }}
